#include <stdio.h>
//선택정렬 O(n^2) 안정성을 만족하지 않음
//삽입정렬 O(n^2) (이미 정렬되어 있는 경우 O(N)) (이미 정렬이 되어있을 때 효과적)
//버블정렬 O(n^2)
//셸 정렬 O(n^1.5)
//병합정렬 O(nlog(n))
//퀵정렬 O(nlog(n))
//기수정렬 O(dn) 0<d<10
int main()
{


}
